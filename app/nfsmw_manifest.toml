# Generated by ReXGlue v0.10.0.2-dev.gc94f5eb on 2026-09-04 02:53:24 UTC.
[project]
name = "nfsmw"
sdk_version = "0.10.0"
game_root = "../assets/game_root"

[entrypoint]
file_path = "../assets/game_root/default.xex"
out_directory_path = "generated/default"

# Includes are processed BEFORE this table, so whatever is set here wins over
# what's included.
#
#   overrides.toml  hand-written: specific findings, with their explanation
#   huecos.toml     generated by HUECOS.bat: the 774 gaps of >= 8 bytes
#
# IF CODEGEN FAILS after adding huecos.toml, remove it from this list and run
# again: some gap will be a data block embedded in .text rather than code.
# The log will say which one; just delete that line from huecos.toml.
includes = ["overrides.toml", "huecos.toml"]
