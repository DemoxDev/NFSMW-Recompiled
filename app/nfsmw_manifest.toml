# Generated by ReXGlue v0.10.0.2-dev.gc94f5eb on 2026-09-04 02:53:24 UTC.
[project]
name = "nfsmw"
sdk_version = "0.10.0"
game_root = "../assets/game_root"

[entrypoint]
file_path = "../assets/game_root/default.xex"
out_directory_path = "generated/default"

# Los includes se procesan ANTES que esta tabla, asi que lo que se ponga aqui
# gana sobre lo incluido.
#
#   overrides.toml  escrito a mano: hallazgos concretos, con su explicacion
#   huecos.toml     generado por HUECOS.bat: los 774 huecos de >= 8 bytes
#
# SI EL CODEGEN FALLA tras anadir huecos.toml, quitalo de esta lista y vuelve a
# lanzar: algun hueco sera un bloque de datos incrustado en .text y no codigo.
# El log dira cual, y basta con borrar esa linea de huecos.toml.
includes = ["overrides.toml", "huecos.toml"]
